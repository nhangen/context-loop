{
  "name": "context-loop",
  "version": "0.1.3",
  "description": "Stop-hook gate that triggers structured summarization + /compact when context fill crosses thresholds.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test && bash tests/gate.test.sh"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/bun": "latest"
  },
  "engines": { "bun": ">=1.1.0" },
  "license": "MIT"
}
